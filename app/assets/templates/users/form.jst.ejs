<form>

  <label for="user-email">Email</label><br>
  <input name="user[email]" id="user-email" type="text" value="<%= user.escape('email') %>" placeholder="example@example.com" required><br><br>

  <label for="user-nickname">Nickname</label><br>
  <input name="user[nickname]" id="user-nickname" type="text" value="<%= user.escape('nickname') %>" placeholder="Super Cool Person" required><br><br>

  <label for="user-password">Password</label><br>
  <input name="user[password]" id="user-password" type="password" placeholder="enter your password" required><br><br>

  <label for="user-city">City</label><br>
  <select name="user[city_id]" id="user-city">
    <% Meeplespace.cities.each(function (city) { %>
      <option value="<%= city.id %>"
         <% if (city.id === user.city_id) { %>
          <%= "selected" %>
         <% } %>
      ><%= city.get('name') %>
      </option>
    <% }); %>
  </select><br><br>

  <button>Let's Play Games!</button>
</form>